import PropTypes from "prop-types";
import UsersList from "../components/UsersList/UsersList";

function Root() {
  return (
    <>
      <UsersList />
    </>
  );
}

Root.propTypes = {};

export default Root;
